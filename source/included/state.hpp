#include "global.hpp"

namespace state{
    extern bool isMenu;
    extern bool isLoading;
    extern bool inLevel;

    extern float currLevel;
    extern float currWorld;

    extern int musicId; //0 = overworld, 1 = underground, 2 = castle
}